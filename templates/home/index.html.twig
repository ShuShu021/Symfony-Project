{% extends 'base.html.twig' %}

{% block title %}Accueil - CourMaker{% endblock %}

{% block body %}
    <section>
        <h1>Bienvenue sur CourMaker</h1>
        <p>Plateforme de démonstration Symfony avec authentification et CRUD.</p>
        <p>
            <a href="{{ path('app_article_list') }}">Voir les articles</a>
            {% if app.user %}
                — Bonjour, {{ app.user.email }}
            {% endif %}
        </p>
    </section>
{% endblock %}
